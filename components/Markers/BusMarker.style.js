'use strict';
import React, {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  marker: {
    height: 20,
    width: 20,
    borderRadius: 10,
    backgroundColor: '#CC580A',
    justifyContent: 'center',
    alignItems: 'center'
  },
  ageText: {
    fontSize: 9,
    color: '#fff'
  }
})

export default styles
